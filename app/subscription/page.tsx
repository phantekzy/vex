import { PricingTable } from "@clerk/nextjs"

/* Subscription section */
const Subscription = () => {
    return (
        <main>
            <PricingTable />
        </main>
    )
}
/* Export section */
export default Subscription
